# Versioning

SemVer: MAJOR.MINOR.PATCH

PATCH: typo fixes, clarity edits
MINOR: new chapter, new character spec, new evals
MAJOR: breaking schema changes, new canon structure

Per-character versioning:
- characters/<name>/PROFILE.md + json files may include "version"